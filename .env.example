# The same Neon database the frontend uses. The checkpointer writes its own
# tables here (see `pnpm db:setup`); everything else it reads is the app's.
DATABASE_URL=

# Same key and the same model chain as the frontend. GEMINI_MODELS replaces the
# chain outright; GEMINI_MODEL names a first choice and keeps the rest behind it.
GEMINI_API_KEY=
GEMINI_MODEL=
GEMINI_MODELS=

# Shared secret the frontend sends as `Authorization: Bearer <token>`. Until the
# frontend is wired up this is also how you call the API by hand.
SERVICE_TOKEN=

# Optional. Only set while testing by hand, so a request need not carry a user id.
DEFAULT_USER_ID=
